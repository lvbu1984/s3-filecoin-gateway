node_modules/
.env
.DS_Store


# VaultX runtime artifacts (do not commit)
mock_uploads/
*.bin
chunk*.bin

# big artifacts (never commit)
uploads_merged/
*.zip

# NOTE: ignore rules will be refined after first end-to-end storage flow works

